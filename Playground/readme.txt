"npm start" runs:

	- node express server on localhost:8779 

		connects to mysql db using bookshelf and retrieves a stored user
		thus far es5 only

	- angular2 server on localhost:3000  

		displays simple message
		uses typescript